import java.util.Scanner;
public class ExamenPRO {

	public static void main(String[] args) {
		/*int lado = 5;
		int apotema = 5;
		int area;
		area = area_pentagono(lado , apotema);
		System.out.println(area);*/
		int [] array;
		array = array();
		for (int i = 0; i < array.length; i++) {
		System.out.println(array[i]);
		}
	}
	
		/*public static int area_pentagono(int lado, int apotema) {
			int area;
			//int perimetro = lado * 5;
			area = (((lado * 5) * apotema)/2);
			return area;
		}*/
	
		public static int[] array () {
			int x = -1;
			Scanner scan = new Scanner(System.in);
			do {
				System.out.println("Introduzca un número entero: ");
				x = scan.nextInt();
				if(x < 0) System.out.println("El número debe ser positivo.");
			} while(x < 0);
			scan.close();
			 int [] array = new int [x * 2];
			 for(int i = 0; i < array.length; i++) {
				 array[i] = x;
				 x++;
			 }
			 return array;
		}
}
